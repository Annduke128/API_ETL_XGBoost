# ⚠️  TEMPLATE FILE - ĐỔI MẬT KHẨU TRƯỚC KHI APPLY
# 
# Cách sử dụng:
# 1. Copy file này thành secrets.yaml (không commit secrets.yaml)
# 2. Thay đổi các giá trị <CHANGE_ME>
# 3. Apply: kubectl apply -f secrets.yaml
# 4. Xóa file secrets.yaml sau khi apply

apiVersion: v1
kind: Secret
metadata:
  name: retail-secrets
  namespace: retail-data
type: Opaque
stringData:
  POSTGRES_USER: "retail_user"
  POSTGRES_PASSWORD: "<CHANGE_ME>"
  CLICKHOUSE_PASSWORD: "<CHANGE_ME>"
  REDIS_PASSWORD: "<CHANGE_ME>"
  EMAIL_PASSWORD: ""
---
apiVersion: v1
kind: Secret
metadata:
  name: retail-secrets
  namespace: ml-pipeline
type: Opaque
stringData:
  POSTGRES_USER: "retail_user"
  POSTGRES_PASSWORD: "<CHANGE_ME>"
  CLICKHOUSE_PASSWORD: "<CHANGE_ME>"
  REDIS_PASSWORD: "<CHANGE_ME>"
  EMAIL_PASSWORD: ""
